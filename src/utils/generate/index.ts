import generateToken from "./generateToken"
import generateSchemaDefaultDefinition from "./generateSchemaDefaultDefinition"

const generate = {
    token: generateToken,
    schemaDefaultDefinition: generateSchemaDefaultDefinition,
};

export default generate;